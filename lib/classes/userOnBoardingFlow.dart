enum UserOnBoardingFlows {
  NA,
  FLOW1 /* condition : when players = 0 && mats = 0  */,
  FLOW2 /* condition : when players != 0 && mats = 0 */,
  FLOW3 /* condition : when players = 0 && mats != 0 */,
}
